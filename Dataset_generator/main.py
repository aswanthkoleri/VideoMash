def FindBestMethod(videoName,subtitleName):
    # Do each algorithm and obtain the subtitle list for each algorithm.
    
    # Take the intersection of all the algorithm

    # Find the algorithm which has the more percentage of sentences which is included in the intersection generated.

    bestMethod=""
    return bestMethod
    
def main():
    # No of videos
    noOfVideos=int(input())
    videos=[]
    for i in videos:
        bestMethod=FindBestMethod(i.videoName,i.subtitleName)
        print(bestMethod)